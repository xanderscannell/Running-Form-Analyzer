import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand colors
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFFBBDEFB);

  // Skeleton overlay colors
  static const Color jointDefault = Color(0xFFFF5722);
  static const Color jointSelected = Color(0xFFFFEB3B);
  static const Color jointManuallyAdjusted = Color(0xFF4CAF50);
  static const Color connectionLine = Color(0xFF00E676);
  static const Color connectionLineSecondary = Color(0xFF69F0AE);

  // Joint category colors
  static const Color faceJoints = Color(0xFF9C27B0);
  static const Color upperBodyJoints = Color(0xFF2196F3);
  static const Color handJoints = Color(0xFF00BCD4);
  static const Color lowerBodyJoints = Color(0xFF4CAF50);
  static const Color feetJoints = Color(0xFFFF9800);

  // UI colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFD32F2F);
  static const Color onPrimary = Colors.white;
  static const Color onSurface = Color(0xFF212121);
  static const Color onSurfaceSecondary = Color(0xFF757575);
}
